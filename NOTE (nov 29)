you can check the main repo for any new changes by

git fetch

to see if your local repo needs to be sync with the remote repo

--if your local repo is not sync with the remote--
git fetch
git branch -r **to see the branches in the remote
git merge origin/<branch>
